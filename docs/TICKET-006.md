# TICKET-006: デプロイ設定・公開（GitHub + Vercel/Netlify）

## 概要

完成したサイトを GitHub にアップロードし、Vercel または Netlify で一般公開する。作業担当はオーナー（Claude がガイドを提供）。

## 対象作業

- GitHubリポジトリへのプッシュ
- Vercel または Netlify の設定・デプロイ

## 依存チケット

- TICKET-001〜005（全フロントエンド実装完了後に着手）

## タスク

### 事前確認
- [ ] 全ページ（index.html・compare.html）をブラウザで開いて動作確認する
- [ ] コンソールエラーがないことを確認する
- [ ] 全27年分のレギュレーション表示・切り替えが正常であることを確認する
- [ ] 任意の2年比較が正常に動作することを確認する（逆順選択の自動修正を含む）

### GitHub
- [ ] GitHubに新しいリポジトリを作成する（公開設定：Public推奨）
- [ ] `git init` → `git add .` → `git commit` を行う
- [ ] `git remote add origin <リポジトリURL>` を設定する
- [ ] `git push origin main` でプッシュする

### Vercel または Netlify 設定
- [ ] Vercel または Netlify のアカウントを用意する（既存アカウント可）
- [ ] GitHubリポジトリを連携する
- [ ] ビルド設定を確認する（静的サイトのためビルドコマンドは不要。公開ディレクトリは `./`）
- [ ] デプロイを実行し、発行URLにアクセスして動作確認する

### 公開後確認
- [ ] 発行URLで全ページが正常に表示されることを確認する
- [ ] 画像・Markdownファイル・diffs.json がすべて読み込まれることを確認する（ネットワークタブで確認）
- [ ] GitHubリポジトリへのプッシュで自動デプロイが動作することを確認する

## 完了条件

- 公開URLでサイトにアクセスでき、全機能が正常に動作する
- GitHub と Vercel/Netlify の自動デプロイが連携されている
